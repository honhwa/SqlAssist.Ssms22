# SqlAssist for SSMS 22

SSMS 22.9.x 的 T-SQL 擴充：Core 與 Metadata 是可測試的 netstandard2.0 純邏輯，
Ssms22 是 net48 VSIX 接線層。

## 工作契約

- **動手前先讀 [文件路由](docs/index.md)，並讀完實際變更範圍列出的護欄。** 規則只是
  按需搬開，沒有取消；跨範圍取聯集。只依檔名、符號與標題讀命中內容，不預載文件樹。
- **概覽用壓縮，讀檔先定位再分段，完整 diff 留給最終審查或必要的正確性確認。**
  命令分工見文件路由的「AI 工作流程」；`rtk proxy` 不壓縮輸出，不當成預設省 token 手段。
- 保留使用者既有變更。完整 diff 與窮盡搜尋不得拿摘要或截斷輸出當證據；大量輸出用
  節流工具，但不能縮小測試範圍、吞掉結束碼或只找「成功」字樣。
- 文件與註解用繁體中文（英文 README 與影像模型提示詞除外）；所有文字檔為 UTF-8 無
  BOM、LF，只有 `.sln` 保留 BOM。
- 回覆只列變更、驗證與未解風險，不重貼程式碼或成功紀錄。
