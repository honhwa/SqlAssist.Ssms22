# 浮動預覽視窗機制

## 為什麼不是工具視窗

視窗掛在編輯器自己的**空間保留管理員**上，也就是 IntelliSense 清單與提示視窗
用的那一套機制，並且排在內建的 `completion` 之後。自訂 Agent 取得前序視窗已經
保留的區域，再用明確矩形定位自己的 WPF `Popup`：

- **位置可預測**：建議清單仍由 reservation geometry 自動量得，但上下左右的
  優先序、文件邊界與 fallback 由 `PreviewPlacementEngine` 的純規則決定。
- **點進去不會關掉清單**：焦點落在這種視窗裡時，編輯器仍然算「持有焦點」，
  所以可以用滑鼠把 `CREATE TABLE` 整段拉選起來，建議清單不會消失。
- **跟著編輯器走**：捲動、切換視窗、關閉查詢視窗時一起處理掉。

不能只換成獨立 `Window` 或一般 Popup：那兩者不在編輯器的聚合焦點裡，點進內容
就會讓 completion 認為編輯器失焦。自訂 Agent 保留平台的生命週期，只有定位改由
本擴充負責。

## 擺放位置

| `preview.placement` | 行為 |
|---|---|
| `beside` | 貼在建議清單的左右任一側，優先右側、撞到邊界才翻 |
| `stacked`（預設） | 擺在建議清單的上下方，從清單錨點延伸到編輯器右側 |

`beside` 依序嘗試右側、左側；兩側都放不下時自動退回上下擺放。`stacked` 依序
嘗試下方、上方。同一次顯示會保留上一個仍然可行的方向，避免提示多一個像素或
DPI 捨入時反覆翻面。所有候選都逐一檢查分離的保留區，不會把兩個遠離的浮窗
粗略合成一個巨大外框。

**蓋住整個錨點的保留區不算障礙。** 建議清單開著時，平台會連錨點所在的一整行
一起保留——文件寬 487–1775 時那一塊就是 `487,100–1775,121`，橫跨整個編輯器而
只有一個行高。它表達的是「錨點在這裡」，不是「這裡有一個浮窗」；而側邊擺放的
上緣永遠對齊錨點上緣，於是每一次都與它相交，左右兩側的自由空間被整條切光，
`beside` 只要清單一開就必定退回上下擺放——縮到最小尺寸也一樣，因為放不下的
不是寬度而是整條帶。判定只排除**完全蓋住**錨點的那一種：改用相交會把緊貼錨點
下緣、DPI 捨入後重疊一兩個像素的建議清單也一起排掉，那才是真正必須讓開的東西。
上下擺放不受影響，該讓開的清單本來就在錨點下方。

打開 `sqlAssist.diagnostics.verboseLogging` 之後，每次顯示都會在記錄檔留下一行落點資訊
（實際方向、視窗與錨點的螢幕像素矩形、文件可用範圍、每一個保留區的矩形）。

一百多個欄位的資料表在 620 寬裡永遠要橫向捲，位置也不穩定，那就改用
`stacked`：左側跟建議清單使用同一個文字錨點，右側不超過編輯器，欄位表也能
一次攤開好幾欄，代價是會蓋住上下幾行程式碼。

`stacked` 尚未手動調寬時會自動延伸到編輯器右側；錨點靠右而放不下正常寬度時，
整個矩形只往左平移到文件界內，不縮成細條，也不讓 Popup 自己翻面。垂直可用範圍
來自完整查詢文件欄與目前螢幕工作區的交集，**不是 SQL 文字 Viewport**；因此開啟
SELECT 結果只代表預覽可以覆蓋結果區，不會把記住的正常高度壓回 180。
文件欄只接受左右界與編輯器吻合的 WPF／Win32 最近父容器；辨識不到時保守退回
文字 Viewport，不拿整個 SSMS 主視窗猜測，以免蓋住底部工具窗或另一個分割文件。

**比對邊界用的是含裝飾邊的編輯器控制項**（WPF 樹上實作 `IWpfTextViewHost` 的那一層），
不是文字 Viewport。Viewport 的左界不含中斷點欄、行號欄與變更欄，右界不含捲軸，
兩邊加起來就超過任何合理的容忍值，於是每一個祖先都被判成不是同一欄——文件欄永遠
找不到，而症狀不是報錯，是「查詢一有結果，預覽的底就卡在文字區底部」。認不出那一層
時基準才退回 Viewport，容忍值也跟著放到整條裝飾邊之外。

`sqlAssist.diagnostics.verboseLogging` 的落點記錄裡有「底界來源」一欄：`wpf`／`win32`
是找到了同欄父容器，`none/*` 則是沒找到而停在編輯器底部——那正是預覽蓋不到結果區時
要先看的一格。

上下與側邊分開保存寬高，而**存到哪一組看實際落點，不看設定值**。側邊放不下退回
上下時，整個定位會用上下那一組尺寸重算一次——連「尚未手動調寬時延伸到編輯器右側」
都跟著成立——拖曳結束也存進上下那一組。沿用側邊的尺寸會讓退回來的視窗帶著一個
側邊才有意義的寬度，而使用者接著拖出來的尺寸又存回側邊那一組，於是側邊記住一個
它根本放不下的寬度，下一次更容易再退回來。雙擊重設同理，重設的是眼前這個視窗那一組。

版面暫時不足而縮小的是 effective rect，永遠不寫回
preferred size：定位引擎會**分兩軸**回報哪一軸被壓縮，被壓縮的那一軸拖曳結束後
不保存。合成一個旗標的話，只要有一軸放不下就會連另一軸使用者真的拖出來的尺寸
一起丟掉。代價是在被壓縮的軸上刻意縮小也不會被記住——那一軸拖不出比限制更大的值，
所以分不出「他想要 350」與「這裡只放得下 400」。

上下擺放尚未手動調寬時，寬度是「自動延伸到編輯器右側」這個**狀態**而不是一個數值，
所以它的門檻不是 0.5 而是一整個握把的邊長：角落握把一定同時動到兩軸，只想拉高的人
也會順手帶進幾個像素的水平位移，而那幾個像素會把自動寬度換成固定值，再也回不去。
其餘三種情況維持 0.5，否則拖完之後尺寸會跳回上一個偏好值。

## 為什麼仍採方角

自訂 Agent 已能控制透明 Popup，但 reservation geometry、可點擊區與實際內容仍以
完整矩形計算。這次重構優先保證定位、焦點與縮放完全一致，不再混入 DWM 裁切。
方角配一條細邊框也與 SSMS 既有浮窗一致；圓角若要做，必須另外連 hit-test 與陰影
一起驗證，不能只改 `CornerRadius`。

顯示時**不會主動搶焦點**，游標留在編輯器裡，可以繼續打字；滑鼠滾輪不需要焦點
就能捲動，要拉選文字時點一下才把焦點交出去。
